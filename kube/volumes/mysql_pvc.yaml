---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: amz-selenium-web-mysql
  labels:
    app: amz-selenium-web
    component: mysql
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: standard
