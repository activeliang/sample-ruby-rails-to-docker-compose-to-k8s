---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: amz-selenium-web-uploads
  labels:
    app: amz-selenium-web
    component: uploads
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: standard
